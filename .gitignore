# Build artifacts
/bin/
/dist/
*.test
*.out
*.prof
coverage.txt
coverage.html
# Stray binaries from `go build ./...`
/router
/replay
/gen-traces

# Go
vendor/

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Models and weights (never commit; large)
/models/
*.gguf
*.bin
*.safetensors

# Bench raw outputs (commit only summarized results.md)
/bench/results/
/bench/results-*/
/bench/out/

# Local copies of cloud bench data pulled by scp. The per-model trees
# the chart scripts read from live here; kept locally but never committed.
# What enters the repo is the summarized real.md and the rendered PNGs
# in docs/.
/bench-results-cloud*/
/bench-results-sweep*/

# AI assistant scratch (local only)
CLAUDE.md
.claude/
.claude*
*.claude.md
.aider*
.cursor*
